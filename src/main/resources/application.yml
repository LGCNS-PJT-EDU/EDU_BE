spring:
  application:
    name: takeit

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true

    h2:
      console:
        enabled: true
        path: /h2-console

  security:
    user:
      name: takeit
      password: takeit

  redis:
    host: ${SPRING_REDIS_HOST:redis}
    port: ${SPRING_REDIS_PORT:6379}
  cache:
    type: redis

kakao:
  client-id: 0257e0d9342333ce55ef60c412d20c5f
  client-secret: n6XfSXYOF595aQ2yJd3ZU6U5S1a3GXuY
  redirect-url: http://localhost:8080/oauth/kakao/callback
  token-uri: https://kauth.kakao.com/oauth/token
  jwk-uri: https://kauth.kakao.com/.well-known/jwks.json


management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      show-details: always
